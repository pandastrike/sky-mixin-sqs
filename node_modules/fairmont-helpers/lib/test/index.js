// Generated by CoffeeScript 1.12.4
(function() {
  var i, len, target, targets;

  targets = process.argv.slice(2);

  if (targets.length === 0) {
    targets = ["array", "hash", "numeric", "object", "promise", "string", "type", "util"];
  }

  for (i = 0, len = targets.length; i < len; i++) {
    target = targets[i];
    require("./" + target);
  }

}).call(this);
